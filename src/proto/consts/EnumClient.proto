syntax = "proto3";

package protobuf.consts;

option java_outer_classname = "EnumClientProto";

enum EnumClient {
  pc = 0;   // windows(和mac互斥)
  mac = 1;  // mac(和windows互斥)
  ipad = 2; // ipad
  app = 3;  // app
}